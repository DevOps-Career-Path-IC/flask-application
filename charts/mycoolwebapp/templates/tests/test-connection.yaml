apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mycoolwebapp.fullname" . }}-test-connection"
  labels:
    {{- include "mycoolwebapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mycoolwebapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
