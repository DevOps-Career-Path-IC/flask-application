apiVersion: v1
kind: Service
metadata:
  name: {{ include "mycoolwebapp.fullname" . }}
  labels:
    {{- include "mycoolwebapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mycoolwebapp.selectorLabels" . | nindent 4 }}
